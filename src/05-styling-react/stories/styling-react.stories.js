import React from 'react';
import AppCSSModules from '../css-modules/App'
import AppStyledComponents from '../styled-components/App'

export default {
  title: 'Styling React'
};

export const CSSModules = () => (
  <AppCSSModules />
);

export const StyledComponents = () => (
  <AppStyledComponents />
);